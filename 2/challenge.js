function divisor(a, b) {
    // Retorna true se b é um divisor (um fator de) de a
}

function isOdd(a) {
    // Retorna true se a for ímpar
    // PS: Tente usar a função "divisor" aqui
}

function areaOfRectangle(w, h) {
    // Retorna a área de um retângulo com largura w e altura h
}

function areaOfCircle(r) {
    // Retorna a área de um círculo de raio r
}

function isPrime(a) {
    // Retorna true se a for primo
}