function sum(a, b) {
    // Retorna a mais b
}

function subtract(a, b) {
    // Retorna a menos b
}

function multiply(a, b) {
    // Retorna a vezes b
}
function divide(a, b) {
    // Retorna a dividido por b
}