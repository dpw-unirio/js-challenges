function hasAtSign(str) {
    // Retorna true se existe um arroba em str
}

function isValidEmail(str) {
    // Retorna true se str é um email válido
    // Consideraremos um email válido uma string que possui o formato:
    // {texto}@{texto}.{tld}
}


function palindrome(str) {
    // Retorna true se str é um palíndromo
}